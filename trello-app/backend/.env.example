# バックエンド（ローカル Node から Docker の Postgres へ接続）
PORT=3001
DATABASE_URL=postgresql://trello:trello@localhost:5432/trello

# docker-compose の DB 認証を変えた場合は、上記 URL のユーザー・パスワード・DB 名を合わせてください。
# バックエンドを将来コンテナ化する場合: ホスト名は `db` にできます。
